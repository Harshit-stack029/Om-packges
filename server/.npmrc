# multer-storage-cloudinary@4 declares a peer dep on cloudinary@^1.x, but we
# use cloudinary@^2 directly (only the v2 API is touched in config/cloudinary.js
# — multer-storage-cloudinary still works with v2 at runtime). Strict npm ci
# refuses to install without this flag.
legacy-peer-deps=true
